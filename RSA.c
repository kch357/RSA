#include <stdio.h>
#include <gmp.h>

int main() {
    //N을 구한다. L을 구한다. E를 구한다. D를 구한다.
    char plaintext;
    mpz_t p, q;
    mpz_init_set_str(p, "12378123721083", 10);
    while (1)
    {
        
    }
    
    
    return 0;
}